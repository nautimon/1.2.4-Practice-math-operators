Write an expression that uses at least three different arithmetic operators to log the number 4 to the console.

For example: 

```
console.log(1 + 5 - 2 * 1);  // But try some other operators too!
```